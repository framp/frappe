{
  "name": "frappe",
  "scripts": {
    "prettify": "prettier --write *.mjs",
    "lint": "standard",
    "test": "node --experimental-modules element"
  },
  "devDependencies": {
    "prettier": "^1.14.2",
    "standard": "^12.0.1"
  }
}
